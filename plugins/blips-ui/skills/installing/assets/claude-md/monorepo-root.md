<!--
Template: bloco de PONTEIRO para o CLAUDE.md da RAIZ de um MONOREPO.
A seção completa de UI vai no CLAUDE.md do app consumidor (template
monorepo-app.md) — a raiz só ganha este bullet, inserido na lista da seção de
stack existente (ex.: "## Stack"). Adapte `apps/web` ao caminho real do app.
Não crie uma seção nova na raiz só para isso.
-->

<!-- blips-ui:claude-md-root:start -->
- **UI** (`apps/web`): `@blips/ui` (design system Blips — shadcn/Radix +
  Tailwind v4). Convenções completas em `apps/web/CLAUDE.md` (seção
  "UI — @blips/ui") e nas rules de UI em `.claude/rules/` (globs escopados ao
  app). Docs: https://blips-ui.web.app
<!-- blips-ui:claude-md-root:end -->
