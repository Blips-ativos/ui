---
title: Input
description: An input component with consistent styling.
---

import { Input } from "@blips/ui";

# Input

An input component with consistent styling for form fields.

## Installation

```bash
pnpm add @blips/ui
```

## Usage

```tsx
import { Input } from "@blips/ui";

export function MyComponent() {
  return <Input type="email" placeholder="Email" />;
}
```

## Examples

### Default

<div className="my-6 max-w-sm">
  <Input placeholder="Enter your email" />
</div>

```tsx
<Input placeholder="Enter your email" />
```

### With Label

<div className="my-6 max-w-sm space-y-2">
  <label htmlFor="email" className="text-sm font-medium">Email</label>
  <Input id="email" type="email" placeholder="name@example.com" />
</div>

```tsx
<div className="space-y-2">
  <label htmlFor="email" className="text-sm font-medium">Email</label>
  <Input id="email" type="email" placeholder="name@example.com" />
</div>
```

### Disabled

<div className="my-6 max-w-sm">
  <Input disabled placeholder="Disabled input" />
</div>

```tsx
<Input disabled placeholder="Disabled input" />
```

### File Input

<div className="my-6 max-w-sm">
  <Input type="file" />
</div>

```tsx
<Input type="file" />
```

## API Reference

### Props

The Input component accepts all standard HTML input attributes.

| Prop | Type | Default | Description |
|------|------|---------|-------------|
| `type` | `string` | `"text"` | The type of input |
| `placeholder` | `string` | - | Placeholder text |
| `disabled` | `boolean` | `false` | Whether the input is disabled |
| `className` | `string` | - | Additional CSS classes |
