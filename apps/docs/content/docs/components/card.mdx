---
title: Card
description: A card component with header, content, and footer sections.
---

import { Card, CardHeader, CardTitle, CardDescription, CardContent, CardFooter, Button } from "@blips/ui";

# Card

A card component with header, content, and footer sections for organizing content.

## Installation

```bash
pnpm add @blips/ui
```

## Usage

```tsx
import {
  Card,
  CardHeader,
  CardTitle,
  CardDescription,
  CardContent,
  CardFooter,
} from "@blips/ui";

export function MyComponent() {
  return (
    <Card>
      <CardHeader>
        <CardTitle>Card Title</CardTitle>
        <CardDescription>Card Description</CardDescription>
      </CardHeader>
      <CardContent>
        <p>Card Content</p>
      </CardContent>
      <CardFooter>
        <p>Card Footer</p>
      </CardFooter>
    </Card>
  );
}
```

## Examples

### Basic Card

<Card className="my-6 max-w-sm">
  <CardHeader>
    <CardTitle>Create project</CardTitle>
    <CardDescription>Deploy your new project in one-click.</CardDescription>
  </CardHeader>
  <CardContent>
    <p className="text-sm text-muted-foreground">
      Your new project will be created with all the default settings.
    </p>
  </CardContent>
  <CardFooter className="flex justify-between">
    <Button variant="outline">Cancel</Button>
    <Button>Deploy</Button>
  </CardFooter>
</Card>

```tsx
<Card className="max-w-sm">
  <CardHeader>
    <CardTitle>Create project</CardTitle>
    <CardDescription>Deploy your new project in one-click.</CardDescription>
  </CardHeader>
  <CardContent>
    <p className="text-sm text-muted-foreground">
      Your new project will be created with all the default settings.
    </p>
  </CardContent>
  <CardFooter className="flex justify-between">
    <Button variant="outline">Cancel</Button>
    <Button>Deploy</Button>
  </CardFooter>
</Card>
```

## API Reference

### Card

The main container component.

### CardHeader

Container for the title and description.

### CardTitle

The title of the card.

### CardDescription

A description below the title.

### CardContent

The main content area.

### CardFooter

The footer area, typically for actions.
