import { defineConfig } from "tsup";

export default defineConfig({
  entry: ["src/index.ts"],
  format: ["cjs", "esm"],
  dts: true,
  splitting: false,
  sourcemap: true,
  clean: true,
  external: [
    "react",
    "react-dom",
    "next-themes",
    /^@radix-ui\/.*/,
    /^@hookform\/.*/,
    "cmdk",
    "sonner",
    "vaul",
    "embla-carousel-react",
    "react-day-picker",
    "react-hook-form",
    "react-resizable-panels",
    "recharts",
    "input-otp",
    "lucide-react",
    "zod",
    "date-fns",
  ],
  treeshake: true,
  minify: false,
});
