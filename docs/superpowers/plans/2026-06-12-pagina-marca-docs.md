# Página "Marca" nos docs — Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Adicionar uma aba "Marca" ao site de docs (`apps/docs`) que exibe os logos/b-mark ao vivo (via `@blips/brand`), as cores da marca, downloads dos arquivos e diretrizes de uso.

**Architecture:** Página única MDX (`content/docs/brand/index.mdx`) sob uma nova aba do header. Reusa `<ComponentPreview>` para os exemplos de código do `<Logo/>` e adiciona 2 componentes custom (`<ColorTokens/>`, `<BrandAssets/>`) para cores e downloads. Os arquivos da marca são copiados de `packages/brand/assets` para `apps/docs/public/brand` por um script de sync encaixado antes do `build-registry` (SSG).

**Tech Stack:** Next 16 (`output: "export"`), fumadocs 16, React 19, Tailwind v4, `@blips/brand` (workspace), `@phosphor-icons/react`, Biome.

**Notas de processo:**
- `apps/docs` não tem test runner — a "verificação" de cada tarefa é build/typecheck/biome/render, não testes unitários.
- Crie uma branch de feature antes de começar (ex.: `feat/docs-pagina-marca`). Commits por tarefa; **não** abra PR nem dê push até o usuário pedir.
- Comentários em pt-BR; mensagens de commit em inglês (Conventional Commits).

---

### Task 1: Wiring do workspace (@blips/brand no docs + export package.json + scripts)

**Files:**
- Modify: `packages/brand/package.json` (exports)
- Modify: `apps/docs/package.json` (deps + scripts)

- [ ] **Step 1: Expor `./package.json` no `@blips/brand`**

Em `packages/brand/package.json`, no objeto `exports`, adicione a entrada (depois de `"./assets/*"`):

```json
    "./assets/*": "./assets/*",
    "./package.json": "./package.json"
```

- [ ] **Step 2: Adicionar a dependência no docs**

Em `apps/docs/package.json`, em `dependencies`, adicione (mantendo ordem alfabética, antes de `@blips/tailwind-config`):

```json
    "@blips/brand": "workspace:*",
    "@blips/tailwind-config": "workspace:*",
```

- [ ] **Step 3: Encaixar o sync no chain de scripts**

Em `apps/docs/package.json`, substitua os scripts `dev` e `build`:

```json
    "dev": "tsx scripts/sync-brand-assets.ts && tsx scripts/build-registry.ts && next dev --webpack",
    "build": "tsx scripts/sync-brand-assets.ts && tsx scripts/build-registry.ts && next build --webpack",
```

- [ ] **Step 4: Instalar e verificar a resolução do pacote**

Run:
```bash
pnpm install
node -e "const r=require('node:module').createRequire('$PWD/apps/docs/'); console.log(r.resolve('@blips/brand/package.json'))"
```
Expected: imprime o caminho `.../packages/brand/package.json` (sem erro de exports).

- [ ] **Step 5: Commit**

```bash
git add packages/brand/package.json apps/docs/package.json pnpm-lock.yaml
git commit -m "chore(docs): wire @blips/brand workspace dep + assets sync hook"
```

---

### Task 2: Script de sync de assets + gitignore

**Files:**
- Create: `apps/docs/scripts/sync-brand-assets.ts`
- Modify: `.gitignore` (raiz)

- [ ] **Step 1: Criar o script de sync**

Create `apps/docs/scripts/sync-brand-assets.ts`:

```ts
import { cpSync, mkdirSync, rmSync } from "node:fs";
import { createRequire } from "node:module";
import { dirname, join } from "node:path";

// Resolve a raiz do @blips/brand pelo package.json (exposto no exports map)
const require = createRequire(import.meta.url);
const brandRoot = dirname(require.resolve("@blips/brand/package.json"));
const src = join(brandRoot, "assets");
const dest = join(process.cwd(), "public", "brand");

// Idempotente: limpa o destino e copia tudo de assets/
rmSync(dest, { recursive: true, force: true });
mkdirSync(dest, { recursive: true });
cpSync(src, dest, { recursive: true });

console.log(`[brand] assets sincronizados → ${dest}`);
```

- [ ] **Step 2: Ignorar o output gerado**

Em `.gitignore` (raiz), adicione abaixo da linha `apps/docs/lib/__registry__.ts`:

```
# Generated by apps/docs/scripts/sync-brand-assets.ts
apps/docs/public/brand/
```

- [ ] **Step 3: Rodar o sync e verificar**

Run:
```bash
cd apps/docs && pnpm exec tsx scripts/sync-brand-assets.ts && cd ../..
find apps/docs/public/brand -type f | sort
```
Expected: lista os 12 arquivos (logo/*, mark/*, favicon/*) copiados de `packages/brand/assets`.

- [ ] **Step 4: Confirmar que não é versionado**

Run: `git status --porcelain apps/docs/public/brand`
Expected: saída vazia (ignorado pelo .gitignore).

- [ ] **Step 5: Commit**

```bash
git add apps/docs/scripts/sync-brand-assets.ts .gitignore
git commit -m "feat(docs): add brand assets sync script"
```

---

### Task 3: Exemplos ao vivo do `<Logo/>` (ComponentPreview)

**Files:**
- Create: `apps/docs/examples/logo-full.tsx`
- Create: `apps/docs/examples/logo-mark.tsx`
- Create: `apps/docs/examples/logo-mark-circle.tsx`
- Create: `apps/docs/examples/logo-recolor.tsx`

- [ ] **Step 1: Criar `logo-full.tsx`**

```tsx
"use client";
import { Logo } from "@blips/brand";

export default function LogoFull() {
  return <Logo variant="full" className="h-10" />;
}
```

- [ ] **Step 2: Criar `logo-mark.tsx`**

```tsx
"use client";
import { Logo } from "@blips/brand";

export default function LogoMark() {
  return <Logo variant="mark" className="size-16 text-primary" />;
}
```

- [ ] **Step 3: Criar `logo-mark-circle.tsx`**

```tsx
"use client";
import { Logo } from "@blips/brand";

export default function LogoMarkCircle() {
  return <Logo variant="mark-circle" className="size-16" />;
}
```

- [ ] **Step 4: Criar `logo-recolor.tsx`**

```tsx
"use client";
import { Logo } from "@blips/brand";

export default function LogoRecolor() {
  return (
    <div className="flex items-center gap-6">
      <Logo variant="mark" className="size-12 text-primary" />
      <Logo variant="mark" className="size-12 text-foreground" />
      <Logo variant="mark" className="size-12 text-muted-foreground" />
    </div>
  );
}
```

- [ ] **Step 5: Buildar `@blips/brand` e o registry; verificar nomes**

Run:
```bash
pnpm --filter @blips/brand build
cd apps/docs && pnpm exec tsx scripts/build-registry.ts && cd ../..
grep -oE '"logo-(full|mark|mark-circle|recolor)"' apps/docs/lib/__registry__.ts | sort -u
```
Expected: as 4 chaves aparecem no `__registry__.ts`.

- [ ] **Step 6: Commit**

```bash
git add apps/docs/examples/logo-full.tsx apps/docs/examples/logo-mark.tsx apps/docs/examples/logo-mark-circle.tsx apps/docs/examples/logo-recolor.tsx
git commit -m "feat(docs): add Logo preview examples"
```

---

### Task 4: Componente `<ColorTokens/>`

**Files:**
- Create: `apps/docs/components/brand/color-tokens.tsx`

- [ ] **Step 1: Criar o componente (client)**

```tsx
"use client";

import { BLIPS_YELLOW, BLIPS_YELLOW_OKLCH } from "@blips/brand";
import { cn } from "@blips/ui/lib/utils";
import { Check, Copy } from "@phosphor-icons/react";
import { useState } from "react";

type Token = { value: string; hint: string };

const TOKENS: Token[] = [
  { value: BLIPS_YELLOW, hint: "Hex" },
  { value: BLIPS_YELLOW_OKLCH, hint: "OKLCH · token --primary" },
];

export function ColorTokens() {
  return (
    <div className="not-prose grid gap-3 sm:grid-cols-2">
      <div className="overflow-hidden rounded-lg border">
        <div
          className="h-28 w-full"
          style={{ backgroundColor: BLIPS_YELLOW }}
        />
        <div className="flex items-center justify-between gap-2 p-3">
          <span className="font-medium text-sm">Amarelo Blips</span>
          <span className="text-muted-foreground text-xs">primary</span>
        </div>
      </div>
      <div className="grid content-start gap-2">
        {TOKENS.map((token) => (
          <CopyRow key={token.value} token={token} />
        ))}
      </div>
    </div>
  );
}

function CopyRow({ token }: { token: Token }) {
  const [copied, setCopied] = useState(false);

  async function copy() {
    await navigator.clipboard.writeText(token.value);
    setCopied(true);
    setTimeout(() => setCopied(false), 1500);
  }

  return (
    <button
      type="button"
      onClick={copy}
      aria-label={`Copiar ${token.hint}: ${token.value}`}
      className={cn(
        "flex items-center justify-between gap-3 rounded-md border px-3 py-2 text-left transition-colors hover:bg-accent"
      )}
    >
      <span className="flex flex-col">
        <code className="font-mono text-sm">{token.value}</code>
        <span className="text-muted-foreground text-xs">{token.hint}</span>
      </span>
      {copied ? (
        <Check className="size-4 text-primary" aria-hidden />
      ) : (
        <Copy className="size-4 text-muted-foreground" aria-hidden />
      )}
    </button>
  );
}
```

- [ ] **Step 2: Verificar typecheck**

Run: `pnpm --filter @blips/docs typecheck`
Expected: sem erros.

- [ ] **Step 3: Commit**

```bash
git add apps/docs/components/brand/color-tokens.tsx
git commit -m "feat(docs): add ColorTokens brand component"
```

---

### Task 5: Componente `<BrandAssets/>`

**Files:**
- Create: `apps/docs/components/brand/brand-assets.tsx`

- [ ] **Step 1: Criar o componente (client; grid de download)**

```tsx
"use client";

import { cn } from "@blips/ui/lib/utils";
import { DownloadSimple } from "@phosphor-icons/react";

type Asset = {
  name: string;
  file: string; // relativo a /brand
  format: string;
  on: "light" | "dark" | "both";
};

type Group = { title: string; assets: Asset[] };

const GROUPS: Group[] = [
  {
    title: "Logos",
    assets: [
      { name: "Logo · texto preto", file: "logo/blips-logo.svg", format: "SVG", on: "light" },
      { name: "Logo · texto branco", file: "logo/blips-logo-white.svg", format: "SVG", on: "dark" },
      { name: "Logo · texto preto", file: "logo/blips-logo.png", format: "PNG", on: "light" },
      { name: "Logo · texto branco", file: "logo/blips-logo-white.png", format: "PNG", on: "dark" },
    ],
  },
  {
    title: "Símbolo",
    assets: [
      { name: "b-mark", file: "mark/blips-mark.svg", format: "SVG", on: "both" },
      { name: "b-mark · círculo", file: "mark/blips-mark-circle.svg", format: "SVG", on: "both" },
      { name: "b-mark", file: "mark/blips-mark.png", format: "PNG", on: "both" },
    ],
  },
  {
    title: "Favicon & ícones",
    assets: [
      { name: "favicon", file: "favicon/favicon.svg", format: "SVG", on: "both" },
      { name: "favicon", file: "favicon/favicon.ico", format: "ICO", on: "both" },
      { name: "apple-touch-icon", file: "favicon/apple-touch-icon.png", format: "PNG", on: "both" },
      { name: "icon 192", file: "favicon/icon-192.png", format: "PNG", on: "both" },
      { name: "icon 512", file: "favicon/icon-512.png", format: "PNG", on: "both" },
    ],
  },
];

export function BrandAssets() {
  return (
    <div className="not-prose flex flex-col gap-8">
      {GROUPS.map((group) => (
        <section key={group.title} className="flex flex-col gap-3">
          <h3 className="font-semibold text-sm">{group.title}</h3>
          <div className="grid gap-3 sm:grid-cols-2 lg:grid-cols-3">
            {group.assets.map((asset) => (
              <AssetCard key={`${asset.file}`} asset={asset} />
            ))}
          </div>
        </section>
      ))}
    </div>
  );
}

function AssetCard({ asset }: { asset: Asset }) {
  const src = `/brand/${asset.file}`;
  const tile =
    asset.on === "dark"
      ? "bg-neutral-900"
      : asset.on === "light"
        ? "bg-white"
        : "bg-[linear-gradient(90deg,#ffffff_50%,#171717_50%)]";

  return (
    <div className="flex flex-col overflow-hidden rounded-lg border">
      <div className={cn("flex h-28 items-center justify-center p-4", tile)}>
        {/* asset estático servido de public/brand (SSG) */}
        {/* biome-ignore lint/a11y/useAltText: alt definido abaixo */}
        <img src={src} alt={`${asset.name} (${asset.format})`} className="max-h-16 w-auto" />
      </div>
      <div className="flex items-center justify-between gap-2 border-t p-3">
        <span className="flex flex-col">
          <span className="font-medium text-sm">{asset.name}</span>
          <span className="text-muted-foreground text-xs">{asset.format}</span>
        </span>
        <a
          href={src}
          download
          aria-label={`Baixar ${asset.name} em ${asset.format}`}
          className="inline-flex size-8 items-center justify-center rounded-md border transition-colors hover:bg-accent"
        >
          <DownloadSimple className="size-4" aria-hidden />
        </a>
      </div>
    </div>
  );
}
```

> Nota: se o Biome reclamar do `<img>` (regra `noImgElement`/Next), o `<img>` é
> correto aqui — em `output: export` com `images.unoptimized`, `next/image` não
> traz ganho e complica o download. Se necessário, adicione o `biome-ignore`
> apropriado em vez de trocar por `next/image`.

- [ ] **Step 2: Verificar typecheck**

Run: `pnpm --filter @blips/docs typecheck`
Expected: sem erros.

- [ ] **Step 3: Commit**

```bash
git add apps/docs/components/brand/brand-assets.tsx
git commit -m "feat(docs): add BrandAssets download grid component"
```

---

### Task 6: Registrar os componentes no MDX

**Files:**
- Modify: `apps/docs/components/mdx-components.tsx`

- [ ] **Step 1: Importar os componentes**

No topo de `apps/docs/components/mdx-components.tsx`, junto dos outros imports `@/components/*`, adicione:

```tsx
import { BrandAssets } from "@/components/brand/brand-assets";
import { ColorTokens } from "@/components/brand/color-tokens";
```

- [ ] **Step 2: Registrar no mapa `mdxComponents`**

No objeto exportado `mdxComponents` (onde estão `ComponentPreview` e `Kbd`), adicione as duas chaves:

```tsx
  ComponentPreview,
  Kbd,
  ColorTokens,
  BrandAssets,
```

- [ ] **Step 3: Verificar typecheck**

Run: `pnpm --filter @blips/docs typecheck`
Expected: sem erros.

- [ ] **Step 4: Commit**

```bash
git add apps/docs/components/mdx-components.tsx
git commit -m "feat(docs): register ColorTokens and BrandAssets in MDX"
```

---

### Task 7: Conteúdo da página + meta

**Files:**
- Create: `apps/docs/content/docs/brand/meta.json`
- Create: `apps/docs/content/docs/brand/index.mdx`
- Modify: `apps/docs/content/docs/meta.json`

- [ ] **Step 1: Criar o meta da seção**

Create `apps/docs/content/docs/brand/meta.json`:

```json
{
  "title": "Marca",
  "pages": ["index"]
}
```

- [ ] **Step 2: Incluir "brand" no meta raiz**

Em `apps/docs/content/docs/meta.json`, adicione `"brand"` ao array `pages` (ao final):

```json
{
  "title": "Documentação",
  "pages": ["index", "components", "skills", "brand"]
}
```

> Se o `pages` atual divergir, apenas acrescente `"brand"` mantendo os existentes.

- [ ] **Step 3: Criar a página**

Create `apps/docs/content/docs/brand/index.mdx`:

```mdx
---
title: Marca
description: Logos, símbolo, cores e diretrizes da marca Blips, com download dos arquivos oficiais.
---

A identidade da Blips gira em torno do **b-mark** e do amarelo `#FCBA28`. Use os
ativos oficiais abaixo — não redesenhe nem recolora o símbolo.

## Logo

O componente `<Logo/>` (do pacote `@blips/brand`) renderiza a marca em SVG
inline, vetorial e recolorível. Três variantes:

<ComponentPreview name="logo-full" />

A variante `mark` herda a cor via `currentColor` — combine com utilitárias de
texto do tema:

<ComponentPreview name="logo-recolor" />

<ComponentPreview name="logo-mark" />

<ComponentPreview name="logo-mark-circle" />

### API

| Prop | Valores | Padrão |
| --- | --- | --- |
| `variant` | `full` · `mark` · `mark-circle` | `full` |
| `title` | texto do `aria-label` (use `aria-hidden` se decorativo) | `"Blips"` |
| ...`svg` | todas as props de `<svg>` (`className`, `width`, etc.) | — |

No `full`, o círculo e o "b" são fixos (amarelo + branco) e o wordmark usa
`currentColor`, então o texto adapta a light/dark sozinho.

## Cores

<ColorTokens />

## Downloads

<BrandAssets />

## Diretrizes de uso

- **Clear-space:** mantenha uma margem livre ao redor do logo de pelo menos a
  altura do "b".
- **Fundos:** logo de texto preto sobre fundos claros, texto branco sobre fundos
  escuros; o círculo amarelo + b branco funcionam em ambos.
- **Contraste:** garanta contraste suficiente entre o símbolo e o fundo.
- **Não faça:** distorcer/esticar, recolorir o círculo amarelo, rotacionar,
  aplicar sombras/efeitos, ou reconstruir o "b" à mão — use o `<Logo/>` ou os
  arquivos oficiais.
```

- [ ] **Step 4: Verificar que a rota gera**

Run:
```bash
pnpm --filter @blips/brand build
pnpm --filter @blips/docs build
test -f apps/docs/out/docs/brand/index.html && echo "rota /docs/brand gerada"
```
Expected: imprime "rota /docs/brand gerada" e o build conclui sem erro.

- [ ] **Step 5: Commit**

```bash
git add apps/docs/content/docs/brand/meta.json apps/docs/content/docs/brand/index.mdx apps/docs/content/docs/meta.json
git commit -m "feat(docs): add Marca page content"
```

---

### Task 8: Aba "Marca" no header

**Files:**
- Modify: `apps/docs/components/site-header.tsx:13-20` (`NAV_ITEMS`)

- [ ] **Step 1: Adicionar a aba**

Em `NAV_ITEMS`, adicione a 3ª entrada (após "Skills"):

```tsx
const NAV_ITEMS = [
  {
    label: "Componentes",
    href: "/docs/components/accordion",
    match: "/docs/components",
  },
  { label: "Skills", href: "/docs/skills", match: "/docs/skills" },
  { label: "Marca", href: "/docs/brand", match: "/docs/brand" },
];
```

- [ ] **Step 2: Verificar typecheck**

Run: `pnpm --filter @blips/docs typecheck`
Expected: sem erros.

- [ ] **Step 3: Commit**

```bash
git add apps/docs/components/site-header.tsx
git commit -m "feat(docs): add Marca tab to header nav"
```

---

### Task 9: Verificação final + gate de review

**Files:** nenhum (verificação)

- [ ] **Step 1: Build limpo de ponta a ponta**

Run:
```bash
pnpm --filter @blips/brand build && pnpm --filter @blips/docs build
```
Expected: ambos concluem sem erro; `apps/docs/out/docs/brand/index.html` existe.

- [ ] **Step 2: Typecheck + Biome**

Run:
```bash
pnpm --filter @blips/docs typecheck
pnpm exec biome check apps/docs/components/brand apps/docs/examples
```
Expected: sem erros (ou só os `biome-ignore` previstos).

- [ ] **Step 3: Conferência visual (dev server)**

Run: `pnpm --filter @blips/docs dev` e abra `http://localhost:3000/docs/brand`.
Verifique: aba "Marca" no header; previews ao vivo das 4 variantes; swatch +
copiar funcionando; grid de downloads com tiles claro/escuro; cada botão baixa
o arquivo correto; TOC lateral lista Logo/Cores/Downloads/Diretrizes.

- [ ] **Step 4: Gate de conformidade `blips-ui:reviewing`**

Invoque a skill **blips-ui:reviewing** sobre `apps/docs/components/brand/*` e
`apps/docs/examples/logo-*.tsx`. Corrija bloqueantes e revise de novo. Só
considere pronto com `Bloqueantes: 0`.

- [ ] **Step 5: (Opcional) abrir PR** — somente se o usuário pedir.

---

## Self-Review (preenchido)

**Cobertura da spec:**
- U1 nav → Task 8 ✓ · U2 conteúdo/meta → Task 7 ✓ · U3 dep+export+sync+gitignore → Tasks 1–2 ✓ · U4 ColorTokens → Task 4 ✓ · U5 BrandAssets → Task 5 ✓ · U6 examples → Task 3 ✓ · registro MDX → Task 6 ✓ · verificação+gate → Task 9 ✓.

**Placeholders:** nenhum "TBD/TODO"; todo passo de código mostra o código completo.

**Consistência de tipos/nomes:** `ColorTokens`/`BrandAssets` (named exports) batem entre o arquivo, o registro no `mdxComponents` e o uso na MDX; nomes de preview (`logo-full`/`logo-mark`/`logo-mark-circle`/`logo-recolor`) batem entre `examples/*.tsx` e a MDX; paths `/brand/<grupo>/<arquivo>` batem com a árvore de `packages/brand/assets` sincronizada por Task 2.
